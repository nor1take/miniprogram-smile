<view bindtap="goToMySpace" class="border" style="width: {{length}}rpx; height: {{length}}rpx; border-radius: {{length}}rpx; border: 1px solid {{color}};">
  <image class="img" src="{{avatarUrl}}"></image>
</view>